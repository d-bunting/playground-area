import { ShowForDurationDirective } from './show-for-duration.directive';

describe('ShowForDurationDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForDurationDirective(undefined as any);
    expect(directive).toBeTruthy();
  });
});
